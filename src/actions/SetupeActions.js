import { createAction } from 'redux-actions';
import { SET_TYPE_ORDER, SET_OBJECT } from '../constans/setup';

export const setTypeOrder = createAction(SET_TYPE_ORDER);
export const setObject = createAction(SET_OBJECT);

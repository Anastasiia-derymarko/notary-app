import React, { Component } from 'react';
import Setup from './components/Setup';

class App extends Component {    
  render() {
    return (
      <div>
        <Setup />  
      </div>
    );
  }
}

export default (App);